name: action1

on: pull_request

jobs:
  action1:
    runs-on: ubuntu-slim
    steps:
      - name: hello
        run: echo "hello"

      - name: action1
        uses: mikeharder/test-actions/action1@main

      - name: action2
        uses: mikeharder/test-actions/action2@main
